using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AxeThrowReThrow : SkillAprendiz
{
    public AxeThrowReThrow(Animator animator, string skillName) : base(animator, skillName)
    {
    }
}
